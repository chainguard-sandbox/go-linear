query GetWorkflowState($id: String!) {
  workflowState(id: $id) {
    id
    name
    description
    type
    color
    position
    createdAt
    updatedAt
  }
}

query ListWorkflowStates($first: Int, $after: String) {
  workflowStates(first: $first, after: $after) {
    nodes {
      id
      name
      description
      type
      color
      position
      createdAt
    }
    pageInfo {
      hasNextPage
      endCursor
    }
  }
}

query ListWorkflowStatesFiltered($first: Int, $after: String, $filter: WorkflowStateFilter) {
  workflowStates(first: $first, after: $after, filter: $filter) {
    nodes {
      id
      name
      description
      type
      color
      position
      createdAt
    }
    pageInfo {
      hasNextPage
      endCursor
    }
  }
}
