query GetProject($id: String!) {
  project(id: $id) {
    id
    name
    description
    state
    createdAt
    updatedAt
    startedAt
    completedAt
    canceledAt
    targetDate
    progress
    health
    healthUpdatedAt
    url
    icon
    color
    lead {
      id
      name
      email
    }
    teams(first: 10) {
      nodes {
        id
        name
        key
      }
    }
    initiatives(first: 10) {
      nodes {
        id
        name
        status
      }
    }
  }
}

query ListProjects($first: Int, $after: String) {
  projects(first: $first, after: $after) {
    nodes {
      id
      name
      description
      state
      createdAt
      updatedAt
      targetDate
      progress
      health
      url
      icon
      color
    }
    pageInfo {
      hasNextPage
      endCursor
    }
  }
}

query ListProjectsFiltered($first: Int, $after: String, $filter: ProjectFilter) {
  projects(first: $first, after: $after, filter: $filter) {
    nodes {
      id
      name
      description
      state
      createdAt
      updatedAt
      targetDate
      progress
      health
      url
      icon
      color
    }
    pageInfo {
      hasNextPage
      endCursor
    }
  }
}
