package linear

// Version information set via ldflags at build time.
// See Makefile for the full set of -X flags.
var (
	Version      = "dev"
	GitCommit    = "unknown"
	GitTreeState = "unknown"
	BuildDate    = "unknown"
)
